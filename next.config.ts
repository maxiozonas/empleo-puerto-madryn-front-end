import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com"], // Agrega el dominio de Unsplash aquí
  },
};

export default nextConfig;

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Avisos Categoria | Madryn Empleos",
};

export default function CategoriaDetalleLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}